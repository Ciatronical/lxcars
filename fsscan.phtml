<!DOCTYPE html>
<html>
<head>
<meta charset='utf-8' />

<?php
    require_once __DIR__.'/../inc/stdLib.php';
    $menu = $_SESSION['menu'];
    echo $menu['stylesheets'];
    echo $menu['javascripts'];
    //echo $head['JQTABLE'];
    echo $head['THEME'];
    echo $head['T8'];// Übersetzung mit kivi.t8
  ?>
<style>

.amounthead{
  width:80px;
}
#input_amount, #input_amount_search{
  width:70px;
}
.customerhead{
  width:250px;
}
#ac_customer, #ac_customer_search{
  width:240px;
}
.descriptionhead, #input_description, #input_description_search{
  width:450px
}
#input_from_search, #input_to_search{
  width:80px;
}

.table1 td, .table1 th {
  padding: 2px;
  }
.table1 tr:nth-child(even) {
  background-color: #D0CFC9;
}
.headline{
  font-weight: bold;
}

</style>

<script>
  $( document ).ready( function(){
    $( '#headline' ).html( kivi.t8( 'Fahrzeugschein-Scanner' ) );
    let fsmax = 16; //Wieviele Fahrzeugscheine sollen geholt werden
    $.ajax({
        url: 'ajax/fsscan.php',
        data: { action: 'getScans', data:{ 'fsmax': fsmax } },
        type: "POST",
        success: function( data ){
          console.log( data );
          var tableContent = '';
          data.forEach( function( item ){
            tableContent += '<tr class="line"><td style="text-align: right; padding-right: 15px;">' + item.dateFormatted + '</td><td>' + item.registrationNumber + '</td>';
          });
          $("#tbody").empty().append( tableContent );
        },
        error: function(){
          alert( 'getScans() Error');
          //$("#tbody").empty();
        }
      })
  });

</script>

<style>



</style>
</head>

<body>
<?php
    echo $menu['pre_content'];
    echo $menu['start_content'];
?>

  <p id="headline" class="ui-state-highlight ui-corner-all tools" style="margin-top: 20px; padding: 0.6em;"></p>
  <table id="dataTable" class="table1" >
    <col class="amounthead">
    <thead>
      <tr class="listheading">
        <th class="xlistheading" id="date">Datum</th>
        <th class="xlistheading" id="licenseplate">Kennzeichen</th>
      </tr>
    </thead>
    <tbody id="tbody" class="row_entry listrow listrow">
    </tbody>
  </table>

<?php echo $menu['end_content']; ?>


</html>
